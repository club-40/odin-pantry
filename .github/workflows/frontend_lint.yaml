name: frontend_lint

on: [ push ]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - run: cd frontend
      - uses: actions/setup-node@v3
        with:
          node-version: ">=18.0.0"
      - run: npm run lint